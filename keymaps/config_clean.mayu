# Minimal self-contained config for E2E testing
# Bypass potential include/parsing issues with 109_clean.mayu

# Define minimal keys
def key A = 0x1e
def key Tab = 0x0f
def key Space = 0x39
def key _0 = 0x0b
def key R = 0x13
def key F1 = 0x3b
def key LWin = 0x5b
def key LCtrl = 0x1d

# Define substitutions
def subst *A = *Tab
def subst *_0 = *R
def subst *Space = *Space
def subst *F1 = *LWin
def subst *LCtrl = *Space

# Define keymap and window rule
keymap Global
window Default /.*/ : Global